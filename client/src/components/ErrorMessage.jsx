import styled from 'styled-components';

const ErrorMessage = styled.p`
  text-align: center;
  color: #ac2a3f;
  font-size: 0.9rem;
  font-weight: bold;
  margin-top: 0px;
`;

export default ErrorMessage;
